@{
    if (Context.IsWebSocketRequest)
    {
        Microsoft.Web.WebSockets.WebSocketExtensions.AcceptWebSocketRequest(Context, new WinCast.App_Code.SocketHandlers.Viewer());
        return;
    }
}